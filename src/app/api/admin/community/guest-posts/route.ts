import { NextRequest, NextResponse } from "next/server";
import { db } from "@/lib/db";
import { requireAdmin } from "@/app/api/admin/_lib/require-admin";
export const dynamic = "force-dynamic";
export async function GET() { const {error}=await requireAdmin(); if(error)return error; const p=await db.guestPost.findMany({orderBy:{createdAt:"desc"},include:{category:true}}); return NextResponse.json({items:p}); }
export async function POST(req: NextRequest) { const {user,error}=await requireAdmin(); if(error)return error; const {id,action,adminNotes}=await req.json(); const p=await db.guestPost.findUnique({where:{id}}); if(!p)return NextResponse.json({error:"Not found"},{status:404}); const status=action==="approve"?"APPROVED":action==="publish"?"PUBLISHED":"REJECTED"; if(action==="publish"){const sl=(s:string)=>s.toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/(^-|-$)/g,"");let slug=sl(p.title);const ex=await db.blogPost.findUnique({where:{slug}});if(ex)slug=`${slug}-${Date.now().toString(36)}`;await db.blogPost.create({data:{title:p.title,slug,excerpt:p.excerpt,content:p.content,coverImage:p.coverImage,categoryId:p.categoryId,authorId:p.authorId,status:"PUBLISHED",featured:false,readTimeMin:Math.max(3,Math.ceil(p.content.split(" ").length/200)),publishedAt:new Date()}});} const u=await db.guestPost.update({where:{id},data:{status,adminNotes:adminNotes||null,reviewedBy:user!.id,reviewedAt:new Date()}}); return NextResponse.json({ok:true,post:u}); }
